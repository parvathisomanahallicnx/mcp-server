services:
  - type: web
    name: mcp-server
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "uvicorn shopify_mcp_server:app --host 0.0.0.0 --port $PORT"
    envVars:
      - key: MCP_TRANSPORT
        value: sse
